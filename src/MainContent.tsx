import React from 'react';

const MainContent = () => {
    return (
        <div className="flex-grow p-6">
            {/* Add other content here, like sections or cards */}
        </div>
    );
};

export default MainContent;
